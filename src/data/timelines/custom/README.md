# Custom timelines

Put compact timeline JSON files in this directory.

Files matching `*.json` are automatically added to the timeline selector at build/dev time.
Run `npm run data:check` after adding or editing a file.

JSON の形は既存の `src/data/timelines/custom/*.json` を参考にしてください。
