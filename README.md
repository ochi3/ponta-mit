```bash
npm run dev
```

## カスタムデータ

- タイムライン: `src/data/timelines/custom/` に JSON を追加
- スキル: `src/data/skills/custom/` に JSON を追加
- 編集後は `npm run data:check` を実行

開発用のメンテナンス GUI・開発ランチャーは `.gitignore` 対象のため GitHub には含めません。ローカルでのみ利用してください。
